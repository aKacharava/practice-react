# How to use
```
An app where the basics of React.js are being practiced.

For example it shows how components are created, 
how different components can be manipulated by using states, 
how props can be used to give a component different propperties and how props can be used in events.

```
